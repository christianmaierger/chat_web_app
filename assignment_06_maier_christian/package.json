{
  "name": "peerserver",
  "version": "1.0.0",
  "description": "Custom Peer.js server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-client": "open-cli videochat.html",
    "start": "node server.js && jest && bash -c \"start chrome \"$(realpath ./jest/report.html)\"\""
  },
  "author": "Tony from Bamberg HCI chair and Christian Maier",
  "license": "ISC",
  "dependencies": {
    "peer": "^0.5.2"
  },
  "devDependencies": {
    "open-cli": "^7.1.0"
  }
}
